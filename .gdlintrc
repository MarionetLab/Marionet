# GDScript Linter Configuration
# gdlint 使用 YAML 格式

# 禁用的检查规则
# 临时禁用一些规则，稍后逐步修复
disable:
  - trailing-whitespace      # 行尾空格（暂时禁用）
  - class-definitions-order  # 定义顺序（暂时禁用）
  - no-elif-return          # 不必要的 elif（暂时禁用）
  - no-else-return          # 不必要的 else（暂时禁用）

# 行长度限制
max-line-length: 100

# 函数复杂度限制（圈复杂度）
max-function-complexity: 15

# 函数最大参数数量
max-function-parameters: 5

# 函数最大返回语句数量
max-returns: 10  # 放宽到 10（默认 6）

# 排除的目录和文件
exclude:
  - ".godot/**"
  - "addons/gd_cubism/**"

